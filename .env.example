BOT_TOKEN=your_telegram_bot_token
ADMIN_IDS=
# CHANNEL_ID=
PORT=

# webhook = production (default). polling = local dev only (clears production webhook).
BOT_UPDATE_MODE=webhook
WEBHOOK_URL=
WEBHOOK_SECRET=

# --- Database (pick one) ---
# Option A: SQLite (local development, no external DB required)
DB_ENGINE=
SQLITE_PATH=


